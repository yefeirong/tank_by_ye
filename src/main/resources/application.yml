#端口号
server:
  port:
    8080
#druid数据库连接池
type:
    com.alibaba.druid.pool.DruidDataSource
#配置mapper
mybatis:
  mapper-locations:
    classpath:mapper/*.xml

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_local?serverTimezone=GMT%2B8&useSSL=true
    username: root
    password: password
  thymeleaf:
    cache:
      false